INSERT INTO RESERVAS (ID_RESERVA, FECHA_ENTRADA, FECHA_SALIDA, NUM_PERSONAS, CLIENTES_CEDULA, FACTURA_NUM)
VALUES (1, TO_DATE('2020-01-01', 'YYYY-MM-DD'), TO_DATE('2020-02-02', 'YYYY-MM-DD'), 5, 1, 1);

SELECT *
FROM RESERVAS
WHERE CLIENTES_CEDULA = ?;

UPDATE RESERVAS
SET NUM_PERSONAS = NUM_PERSONAS + 1
WHERE NUM_PERSONAS = 1;

DELETE
FROM RESERVAS
WHERE NUM_PERSONAS < 5;